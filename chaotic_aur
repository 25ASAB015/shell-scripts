#!/bin/bash

# prompt:
# quiero que me ayudes a crear un bash script que automatice estos pasos que he hecho manualmente en arch linux, Documenta el codigo y hazlo muy visual para que cualquiera sin base tecnica pueda entender como funciona. 





# Este script automatiza la actualización de Arch Linux y la configuración del repositorio Chaotic-AUR.
# ¡IMPORTANTE! Ejecuta este script con cuidado y asegúrate de entender cada paso.
# Se recomienda tener una copia de seguridad de tus datos importantes.

echo "█████████████████████████████████████████████████████████████████████"
echo "█                                                                   █"
echo "█          🚀 Iniciando el Script de Automatización Arch Linux 🚀          █"
echo "█                                                                   █"
echo "█████████████████████████████████████████████████████████████████████"
echo ""
echo "Este script realizará las siguientes acciones:"
echo "1. Actualizará tu sistema Arch Linux."
echo "2. Configurará el repositorio Chaotic-AUR para acceso a más software."
echo "3. Instalará Google Chrome."
echo ""
read -p "¿Estás listo para continuar? (s/n): " respuesta
if [[ ! "$respuesta" =~ ^[Ss]$ ]]; then
    echo "❌ Operación cancelada por el usuario. ¡Hasta la próxima!"
    exit 1
fi

echo ""
echo "---------------------------------------------------------------------"
echo "⚙️  Paso 1: Actualizando las bases de datos de paquetes y claves del sistema..."
echo "---------------------------------------------------------------------"
echo "Esto asegura que tu sistema esté listo para instalar y actualizar software de forma segura."

# Sincroniza las bases de datos de paquetes (como refrescar una tienda para ver los productos nuevos)
# 'pacman -Sy' le dice a pacman que descargue la lista más reciente de paquetes disponibles.
echo "🔄 Sincronizando bases de datos de paquetes..."
sudo pacman -Sy || { echo "❌ Error al sincronizar pacman. Saliendo."; exit 1; }


# Instala o actualiza el paquete archlinux-keyring (como actualizar las llaves de seguridad de tu casa)
# 'archlinux-keyring' contiene las claves criptográficas para verificar la autenticidad de los paquetes.
echo "🔑 Actualizando las claves de seguridad de Arch Linux (archlinux-keyring)..."
sudo pacman -S archlinux-keyring || { echo "❌ Error al instalar archlinux-keyring. Saliendo."; exit 1; }


# Inicializa el sistema de claves de pacman (prepara un llavero nuevo para tus llaves de seguridad)
# 'pacman-key --init' crea el entorno necesario para manejar las claves.
echo "🔒 Inicializando el sistema de claves de pacman..."
sudo pacman-key --init || { echo "❌ Error al inicializar pacman-key. Saliendo."; exit 1; }

# Rellena el sistema de claves con las claves maestras de Arch Linux (pone las llaves maestras en el llavero)
# 'pacman-key --populate' importa las claves oficiales de los desarrolladores de Arch.
echo "📋 Rellenando el sistema de claves con las claves de Arch Linux..."
sudo pacman-key --populate || { echo "❌ Error al poblar pacman-key. Saliendo."; exit 1; }

echo ""
echo "---------------------------------------------------------------------"
echo "✅ Paso 1 Completado: ¡Tu sistema de paquetes está preparado!"
echo "---------------------------------------------------------------------"
echo ""

echo "---------------------------------------------------------------------"
echo "🔄 Paso 2: Actualizando y reiniciando tu sistema Arch Linux..."
echo "---------------------------------------------------------------------"
echo "Es crucial que tu sistema esté completamente actualizado antes de añadir nuevos repositorios."

# Sincroniza y actualiza completamente el sistema (descarga y actualiza todos los productos de la tienda)
# 'pacman -Syyu' hace una sincronización forzada de las bases de datos y luego actualiza todos los paquetes instalados.
echo "⬆️  Actualizando completamente tu sistema (esto puede tardar un poco)..."
sudo pacman -Syyu || { echo "❌ Error al actualizar el sistema. Saliendo."; exit 1; }


echo ""
echo "---------------------------------------------------------------------"
echo "⚠️  Paso 2 Completado. ¡Reiniciando el sistema para aplicar los cambios!"
echo "---------------------------------------------------------------------"
echo ""

# Reinicia el sistema (como reiniciar tu computadora después de una actualización importante)
# 'reboot' apaga y enciende la computadora. ¡El script se detendrá aquí y continuará después del reinicio!
echo "✨ Reiniciando el sistema en 5 segundos..."
sleep 5
sudo reboot

# --- EL SCRIPT CONTINUARÍA AQUÍ DESPUÉS DEL REINICIO ---
# Sin embargo, los scripts de bash no persisten el estado entre reinicios
# Por lo tanto, esta parte del script DEBERÍA EJECUTARSE MANUALMENTE o con un segundo script
# O ser un script que se ejecute al inicio del sistema si lo deseas automatizar completamente.
# Para este ejercicio, asumimos que el usuario lo ejecutará una segunda vez después del reinicio.

# Mensaje para el usuario para que sepa qué hacer
echo "---------------------------------------------------------------------"
echo "🚨 ATENCIÓN: El sistema se reiniciará. Por favor, ejecuta este script "
echo "             NUEVAMENTE después de que tu sistema se haya reiniciado"
echo "             para continuar con la configuración de Chaotic-AUR."
echo "---------------------------------------------------------------------"
exit 0 # Salimos del script, el resto se ejecutará después del reinicio.


# --- PARTE DEL SCRIPT QUE SE EJECUTA DESPUÉS DEL REINICIO ---
# Idealmente, deberías ejecutar este script una segunda vez tras el reinicio.
# Para fines de demostración, el código sigue aquí.

: <<'END_COMMENT'
# Esto es un comentario multilínea.
# Las siguientes líneas NO se ejecutarán a menos que elimines el 'exit 0' de arriba
# y tengas un mecanismo para que el script continúe después de un reinicio.
# Generalmente, esto se maneja con dos scripts o con un servicio de systemd.

# Solo para fines ilustrativos:
echo ""
echo "---------------------------------------------------------------------"
echo "🚀 Paso 3: Configurando el repositorio Chaotic-AUR (después del reinicio)..."
echo "---------------------------------------------------------------------"
echo "Esto te permitirá acceder a más software que no está en los repositorios oficiales."

# Cambia al directorio donde están las claves del repositorio Chaotic-AUR
# 'cd /run/media/arch/ARCHCRAFT_2/setup_chaotic' mueve tu "ubicación" en la terminal.
echo "📂 Navegando al directorio de las claves Chaotic-AUR..."
# Asegúrate de que esta ruta sea la correcta para tu USB o ubicación de los archivos.
# Por ejemplo, si los archivos están en tu carpeta de descargas, sería 'cd ~/Downloads/setup_chaotic'
# O puedes copiarlos a un lugar fijo antes de ejecutar el script.
# Aquí estoy asumiendo la ruta que mencionaste en tus pasos.
CHAOTIC_SETUP_DIR="/run/media/arch/ARCHCRAFT_2/setup_chaotic"
if [ -d "$CHAOTIC_SETUP_DIR" ]; then
    cd "$CHAOTIC_SETUP_DIR" || { echo "❌ No se pudo acceder al directorio: $CHAOTIC_SETUP_DIR. Verifica la ruta y si el USB está montado."; exit 1; }
    echo "✅ Directorio '$CHAOTIC_SETUP_DIR' encontrado y accedido."
else
    echo "❌ ERROR: El directorio '$CHAOTIC_SETUP_DIR' no existe. Asegúrate de que tu USB esté conectado y la ruta sea correcta."
    echo "Por favor, ajusta la variable CHAOTIC_SETUP_DIR en el script si la ruta es diferente."
    exit 1
fi


# Agrega las claves públicas de Chaotic-AUR al sistema de claves de pacman
# 'pacman-key --add' importa una clave específica.
echo "🔑 Añadiendo las claves públicas de Chaotic-AUR..."
sudo pacman-key --add 67bf8ca6da181643c9723b4ed6c9442437365605.asc || { echo "❌ Error al añadir la primera clave. Saliendo."; exit 1; }
sudo pacman-key --add ef925ea60f33d0cb85c44ad13056513887b78aeb.asc || { echo "❌ Error al añadir la segunda clave. Saliendo."; exit 1; }

# Firma localmente las claves (como darle tu sello de aprobación a esas llaves)
# 'pacman-key --lsign-key' marca una clave como de confianza para tu sistema.
echo "✍️ Firmando localmente las claves de Chaotic-AUR..."
sudo pacman-key --lsign-key 3A40CB5E7E5CBC30 || { echo "❌ Error al firmar la primera clave. Saliendo."; exit 1; }
sudo pacman-key --lsign-key 349BC7808577C592 || { echo "❌ Error al firmar la segunda clave. Saliendo."; exit 1; }
sudo pacman-key --lsign-key 3056513887B78AEB || { echo "❌ Error al firmar la tercera clave. Saliendo."; exit 1; }


echo ""
echo "---------------------------------------------------------------------"
echo "📂 Paso 4: Creando y configurando el espejo de Chaotic-AUR..."
echo "---------------------------------------------------------------------"
echo "Esto le dice a tu sistema dónde encontrar los paquetes de Chaotic-AUR."

# Crea el directorio para los espejos de pacman si no existe (crea un nuevo estante para la tienda)
# 'mkdir -p' crea un directorio y sus directorios padres si no existen.
echo "📁 Creando el directorio para la lista de espejos de Chaotic-AUR..."
sudo mkdir -p /etc/pacman.d/ || { echo "❌ Error al crear el directorio /etc/pacman.d/. Saliendo."; exit 1; }

# Añade la lista de espejos de Chaotic-AUR al archivo de configuración de pacman
# Usamos 'sudo tee -a' para añadir la línea al final del archivo sin abrir un editor.
echo "🌐 Añadiendo la lista de espejos de Chaotic-AUR..."
CHAOTIC_MIRROR_LIST="/etc/pacman.d/chaotic-mirrorlist"
echo 'Server = https://eu.mirrors.unitedwefall.fr/chaotic-aur/$repo/$arch' | sudo tee "$CHAOTIC_MIRROR_LIST" > /dev/null || { echo "❌ Error al añadir el espejo al archivo $CHAOTIC_MIRROR_LIST. Saliendo."; exit 1; }
echo 'Server = https://de.mirrors.unitedwefall.fr/chaotic-aur/$repo/$arch' | sudo tee -a "$CHAOTIC_MIRROR_LIST" > /dev/null || { echo "❌ Error al añadir el espejo al archivo $CHAOTIC_MIRROR_LIST. Saliendo."; exit 1; }
echo 'Server = https://sg.mirrors.unitedwefall.fr/chaotic-aur/$repo/$arch' | sudo tee -a "$CHAOTIC_MIRROR_LIST" > /dev/null || { echo "❌ Error al añadir el espejo al archivo $CHAOTIC_MIRROR_LIST. Saliendo."; exit 1; }
echo 'Server = https://us.mirrors.unitedwefall.fr/chaotic-aur/$repo/$arch' | sudo tee -a "$CHAOTIC_MIRROR_LIST" > /dev/null || { echo "❌ Error al añadir el espejo al archivo $CHAOTIC_MIRROR_LIST. Saliendo."; exit 1; }


# Edita el archivo principal de configuración de pacman para añadir el repositorio Chaotic-AUR
# 'nano' es un editor de texto simple que se abre en la terminal.
echo "✏️  Abriendo el archivo de configuración principal de Pacman (/etc/pacman.conf)."
echo "   AÑADE las siguientes líneas al FINAL del archivo:"
echo ""
echo "   [chaotic-aur]"
echo "   Include = /etc/pacman.d/chaotic-mirrorlist"
echo ""
echo "   Presiona Ctrl+X para salir, luego 'Y' para guardar y Enter para confirmar."
sudo nano /etc/pacman.conf || { echo "❌ Error al abrir pacman.conf con nano. Saliendo."; exit 1; }

echo ""
echo "---------------------------------------------------------------------"
echo "✅ Paso 4 Completado: ¡Chaotic-AUR está configurado!"
echo "---------------------------------------------------------------------"
echo ""

echo "---------------------------------------------------------------------"
echo "🔄 Paso 5: Sincronizando y actualizando el sistema nuevamente (con Chaotic-AUR)..."
echo "---------------------------------------------------------------------"
echo "Ahora que hemos añadido un nuevo repositorio, necesitamos que el sistema lo reconozca."

# Sincroniza y actualiza completamente el sistema (ahora con el nuevo "supermercado" de Chaotic-AUR)
echo "⬆️  Sincronizando y actualizando todo el sistema, incluyendo Chaotic-AUR..."
sudo pacman -Syyu || { echo "❌ Error al sincronizar/actualizar con Chaotic-AUR. Saliendo."; exit 1; }

# Instala los paquetes de claves y espejos de Chaotic-AUR (seguridad y lista de servidores)
# Esto es importante para que pacman confíe en los paquetes de Chaotic-AUR.
echo "🔒 Instalando las claves y la lista de espejos de Chaotic-AUR..."
sudo pacman -S chaotic-keyring chaotic-mirrorlist || { echo "❌ Error al instalar chaotic-keyring/mirrorlist. Saliendo."; exit 1; }

echo ""
echo "---------------------------------------------------------------------"
echo "✅ Paso 5 Completado: ¡Todo actualizado y Chaotic-AUR listo!"
echo "---------------------------------------------------------------------"
echo ""

echo "---------------------------------------------------------------------"
echo "🌐 Paso 6: Instalando Google Chrome..."
echo "---------------------------------------------------------------------"
echo "¡Tu navegador web favorito, listo para usar!"

# Instala Google Chrome (finalmente, instalando el programa que queremos)
echo "🚀 Instalando Google Chrome..."
sudo pacman -S google-chrome || { echo "❌ Error al instalar google-chrome. Saliendo."; exit 1; }


echo ""
echo "█████████████████████████████████████████████████████████████████████"
echo "█                                                                   █"
echo "█          🎉 ¡Felicidades! Script de Automatización Completado 🎉          █"
echo "█                                                                   █"
echo "█████████████████████████████████████████████████████████████████████"
echo "¡Tu sistema Arch Linux está actualizado, Chaotic-AUR configurado y Google Chrome instalado!"
echo ""
END_COMMENT
